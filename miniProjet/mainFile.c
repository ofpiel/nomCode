#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <time.h>
/*#include "line.h"
#include "client.h"
#include "date_recharge.h"*/

int main(void)
{
  /*client ch;
  Date d;
  adr a;
  ligne lg;
  ligne* lgtst=NULL;
  FILE* file=NULL;
  int num;
  client cvl;
 
 
 //lg=saisie_ligne();
 //affichage_ligne(lg);
 //file=ajout_ligne(file);
//lg=chercher_ligne(file,53112233);

 //file=Ajout_code(file);
 //modifier_ligne(file,235);
 
 file=ajout_client(file);
 
// ch=chercher_client(file,130002);
 
  // modifier_client(file,130009);
   
//  supprimer_ligne(file,4937664);
   getch();
  return 0;*/
}